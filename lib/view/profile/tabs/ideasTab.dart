part of '../profileTabs.dart';

class IdeasTab extends StatelessWidget {
  const IdeasTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Page Idée',
        style: const TextStyle(fontSize: 36),
      ),
    );
  }
}
